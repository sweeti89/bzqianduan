# 安全与隐私说明（v1.2.0）

> 审查范围：全部前端源码、构建配置、部署产物、依赖（2026-09 安全审计后整改版）。
> 场景：班委小范围使用的本地优先工具，静态托管于 GitHub Pages，可选 LeanCloud 免费版联机共享。

## 一、隐私红线（本次审计最重要的结论）

**v1.1.2 及更早版本曾把全班真实姓名+学号打包进公开的代码仓库和构建产物。**
v1.2.0 起已彻底整改：代码库与构建产物中不再包含任何真实个人信息，名册改由用户
「导入 Excel」录入，数据只存浏览器本机（localStorage + IndexedDB）。

- 如果你部署过早于 v1.2.0 的版本：请到 GitHub 仓库 Settings 联系支持清除缓存的历史
  分支视图，或直接删除重建仓库；
- 名册含个人信息，**共享网址只发给班委和本班同学**，不要公开传播。

## 二、身份与口令体系

| 层 | 机制 | 说明 |
|---|---|---|
| 设备访问口令 | PBKDF2-SHA256（15 万次迭代）加盐哈希 | 打开应用需输入；sessionStorage 会话解锁 |
| 个人密码（防冒用） | 同上，按成员索引，随联机同步 | 别人选你的身份需输密码；旧版单轮哈希验证成功后自动升级 |
| 角色权限 | 班长/班委/同学 三级 | 精确匹配"班长"职务（副班长等不会被误提权）；UI 层权限，防误操作与随意改动 |

- 忘记访问口令：清空本机数据重置（联机开启时数据可从云端拉回）；
- 忘记个人密码：由班长在名册中重置。

## 三、联机同步的安全边界

1. **同步数据全程加密**：整包数据（名册/班费/事务/密码哈希）经 AES-GCM（256 位，
   PBKDF2 15 万次迭代派生密钥）加密后才上传 LeanCloud——云端和 LeanCloud 平台
   都只能看到密文，解密口令由班委线下共享、绝不入云；
2. **同步口令与访问口令相互独立**，保存在本设备（不随同步上云）；
3. **结构校验**：拉取的云端数据逐 key 校验形状，形状不对的绝不写入本机（防伪造
   数据覆盖真名册）；
4. **整包覆盖、以最后保存为准**：两个班委同时编辑会互相覆盖，尽量避免；本机 15 秒
   内有未上传改动时，自动拉取会让位于本机数据；
5. **Web 安全域名的真实作用**（修正旧版描述）：它只能阻止其他网站"带着你的凭证"
   发起浏览器调用，**不能阻止拿到凭证的人用脚本直连 API**——这正是同步加密存在
   的原因。两层防护建议都做。

## 四、其他保护与已知取舍

- **凭证附件**（支付截图等）：仅存记账人设备 IndexedDB，不上传；单文件 ≤10MB，
  类型白名单（图片/PDF）；清空重置应用时一并删除；
- **XSS**：无 v-html/eval/innerHTML，用户输入全部模板转义；生产构建注入 CSP；
- **备份文件**：包含全部业务数据（不含同步凭证与个人密码），请只发给自己；
- **Service Worker**：网络优先 HTML + 缓存优先带 hash 资源，activate 时清理不再
  被引用的旧缓存；
- **依赖**：SheetJS 官方 0.20.3、leancloud-storage 官方 4.15.2、vant 4.10.2，
  无已知高危滞留；
- **已知取舍**：访问口令/个人密码定位是"防顺手翻看与冒用"，不防专业离线破解
  （PBKDF2 15 万次迭代已显著提高成本）；本地数据未做整体加密存储。
